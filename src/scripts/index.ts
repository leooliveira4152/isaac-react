import completionMarkPositionGetter from "./completionMarkPositionGetter";
import { getUserMarks, saveUserMarks } from "./userMarks";
import { getWindowSize, processWindowSizes } from "./windowSize";

export { completionMarkPositionGetter, getUserMarks, saveUserMarks, getWindowSize, processWindowSizes };
